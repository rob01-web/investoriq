# Manual Export Notes

Use the `.txt` fixtures directly for the current live path.

For future OCR-specific testing, these `.txt` fixtures may be manually converted to PDF outside this package, but the current motherload validation package is intentionally plain `.txt` and `.csv`-style source material.

Do not treat any manually exported PDF as the canonical fixture unless a separate OCR test package is created.
