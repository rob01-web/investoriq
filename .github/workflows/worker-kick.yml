name: InvestorIQ Worker Kick

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes
  workflow_dispatch: {}   # lets you run it manually too

concurrency:
  group: worker-kick
  cancel-in-progress: true

jobs:
  kick:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Call InvestorIQ worker
        run: |
          curl -sS -X POST "https://investoriq.tech/api/admin/run-eligible-jobs-once" \
            -H "x-admin-run-key: ${{ secrets.ADMIN_RUN_KEY }}" \
            -H "Content-Type: application/json" \
            --data "{}"
      - name: Call admin-run-worker
        run: |
          curl -sS -X POST "https://investoriq.tech/api/admin-run-worker" \
            -H "x-admin-run-key: ${{ secrets.ADMIN_RUN_KEY }}" \
            -H "Content-Type: application/json" \
            --data "{}"
