# InvestorIQ Master Plan - 2026-09-13

This file is the current repair-plan authority for the September 13 continuation. Historical `01_MASTER_PLAN.md` remains preserved and should not be deleted or reconstructed.

## Current mission

InvestorIQ remains on launch hold while the September 12 prelaunch architecture findings are repaired in seven bounded slices. The immediate objective is not another production test. The objective is to close Slice 1 completely and correctly as one reusable financial and terminology authority repair across Screening and Underwriting.

Current runtime/code parent before documentation-only handoff updates: `811af341f481b55e24803c2a8c296686669630af` on branch `hotfix-worker-queue-starvation-20260911-r1`.

The owner's local worktree contains the authoritative uncommitted Slice 1A v5 patch. Do not reset, clean, stash, overwrite, or assume GitHub contains those local runtime changes.

## Permanent repair doctrine

- Decision first. Facts before prose.
- Root cause before fixture cosmetics.
- Source truth before copy.
- Calculation authority before harmonized display.
- Shared behavior before page styling.
- Stonebridge is diagnostic evidence, never a patch target.
- No hard page caps.
- No test weakening to manufacture a pass.
- No production action before local repair and certification gates close.
- Commit is the finish line of a slice, not part of the repair loop.

## Required per-slice workflow

1. Finish the entire slice locally.
2. Review every changed line, including new/untracked files.
3. Scan the affected codebase for stale aliases, duplicated authority, contradictory thresholds, dead paths, and all issues the slice is meant to eliminate.
4. Run slice-specific regressions.
5. Run `git diff --check`.
6. Run canonical `npm run qa`.
7. Run the production build.
8. Review the final complete diff again after all fixes and tests.
9. Only when the slice is locally certified should one calculated commit be made.
10. If final review finds another issue, repair it in the same uncommitted slice and repeat the necessary gates. No corrective micro-commit chain.

## Slice 1 - Canonical financial and terminology authority - IN PROGRESS

### Slice 1A current local authority

Slice 1A v5 is preserved uncommitted in the owner's local worktree. It establishes canonical `Operating Cost Coverage Ratio` for the OPEX / GPR metric and removes the invalid operating Break-Even Occupancy semantics from Screening.

Canonical OCCR rules:

- formula: accepted total operating expenses / accepted T12 gross potential rent;
- physical occupancy remains separate;
- no occupancy-minus-OCCR cushion or comparison chart;
- `>75%` Sensitized;
- `>85%` Fragile;
- exactly 75% Stable;
- exactly 85% Sensitized;
- explicit ratio formatting with `2.0 = 200.0%`;
- negative OPEX and non-positive GPR invalid.

### Slice 1B status

Slice 1B is not applied locally. A transactional whole-Slice-1 candidate was attempted yesterday, but a stale H14/H15 cap-rate formatting assertion failed and the runner restored every touched file to exact v5.

The failed assertion expected `Going-In Cap Rate 7.0%`; the current renderer correctly emits `7.00%`. That stale assertion is one symptom, not the whole repair target.

### Additional Slice 1 blockers found after rollback

The next complete Slice 1 repair must also address:

- wrong GPR basis in an institutional integration fixture;
- unavailable OCCR falsely capable of failing the deterministic seal;
- Underwriting customer-surface divergence from canonical Source Truth;
- Quality Manifest missing-value coercion through `Number(null)`;
- overly broad OCCR provenance;
- active historical Break-Even Occupancy logic in `api/_lib/acquisition-memo-v2-document.js`;
- obsolete semantic construction in Underwriting base contracts followed by wrapper repair.

Candidate corrections were proven in a rebuilt sandbox but were not left applied after rollback.

### Slice 1 next action

Before another runner is created, complete the active-path audit. Trace all operating-ratio terminology, calculations, receipts, renderers, validators, manifests, and customer-surface consumers. Separate the retired OPEX/GPR operating label from any legitimately distinct debt-inclusive break-even concepts.

Then create one transactional whole-Slice-1 candidate from exact v5 and prove it with focused tests, repository scans, `git diff --check`, canonical QA, production build, and final diff review.

Do not move to Slice 2 until Slice 1 is fully closed.

## Slice 2 - Worker failure integrity

Primary September 12 audit findings:

- worker schema probe timeouts and opaque invocation failure;
- lexical-scope bug masking the original worker error;
- deterministic publication failures being retried to dead-letter;
- parser recovery out-of-scope source hash usage;
- undefined generator variables such as `canonicalGpr` / acquisition state fallbacks where active.

Required outcome: original failures remain observable, deterministic defects do not consume transient retry budgets, invocation evidence exists before risky work, and no late error masks root cause.

## Slice 3 - Deadline, lease, and recovery architecture

Primary findings:

- expired publishable jobs cannot reliably requeue under the owner fence;
- worker/render/PDF/storage/publication budgets are incompatible;
- heartbeat coverage stops too early;
- long operations are not governed by one end-to-end deadline.

Required outcome: one deadline model, aligned lease/heartbeat behavior, explicit abort/cancel semantics, and durable recovery checkpoints.

## Slice 4 - Queue fairness and checkpoint correctness

Primary findings:

- cursor reset and retry ordering can starve jobs;
- >250 eligible jobs can become invisible to current selection behavior;
- repeatedly deferred jobs can monopolize worker passes;
- extraction checkpoints can falsely claim both parsers complete.

Required outcome: fair bounded claims, durable retry timing/reason, correct checkpoint truth, and family fairness.

## Slice 5 - Artifact and publication contract

Primary findings:

- artifact retry naming mismatch;
- section disposition receipts can be omitted;
- contradictory report identity can be normalized instead of rejected.

Required outcome: one typed artifact/publication contract with explicit identity and receipt completeness.

## Slice 6 - Validators and customer language

Primary findings:

- renderer/validator phrase contradiction;
- stale deterministic report-contract headings and disclosure vocabulary;
- broad financing regex false positives;
- stale renovation validator contract.

Required outcome: shared approved-language authority and validators that reject genuine prohibited assertions rather than valid customer copy.

## Slice 7 - Certification architecture

Primary findings:

- admission doctrine/test conflict must be resolved against owner authority;
- scheduler live state differs from written handoff authority;
- generic 500 paths lose correlation;
- test hooks bypass the real publication lifecycle;
- DocRaptor production mode remains an explicit launch configuration gate.

Required outcome: launch certification proves the real lifecycle through publication, storage, finalization, customer visibility, and governed configuration.

## Standing production holds

Until the owner explicitly authorizes otherwise:

- no merge to `main`;
- no Vercel deployment;
- no production Final Attack Test;
- no migrations;
- no scheduler mutation;
- no Supabase/Storage mutation;
- no Stripe/pricing mutation;
- no replay of historical queued/dead-letter jobs;
- keep DocRaptor in TEST mode.

## Definition of progress

Progress is not another green isolated smoke test. Progress is elimination of duplicated authority and contradictory active paths, with the complete slice passing its regressions, canonical QA, build, and final audit before a single commit.
